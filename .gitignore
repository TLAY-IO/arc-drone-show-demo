# Node
node_modules/
.next/
*.log
npm-debug.log*

# ESP-IDF
build/
build-*/
managed_components/
sdkconfig
sdkconfig.old
dependencies.lock

# Env / secrets
.env
.env.local
.env.*.local
*.env.local
# Private keys. docker-compose defaults OPERATOR_KEY_FILE to
# ./burn-intent-worker/operator.key — i.e. inside this repo — so without these
# lines a `git add -A` would stage the operator private key.
*.key
*.pem
*.p12
secrets/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Mosquitto
cloud/mosquitto/data/
cloud/mosquitto/log/
cloud/web/tsconfig.tsbuildinfo
